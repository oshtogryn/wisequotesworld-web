const LANGS=['en','uk','ru','pl','sv','de','es','fr'];
const PROJECT_ID='wisequotesworld';

function preferred(request){const h=(request.headers.get('Accept-Language')||'').toLowerCase();for(const l of ['uk','ru','pl','sv','de','es','fr','en'])if(h.startsWith(l)||h.includes(','+l)||h.includes(' '+l))return l;return'en'}
function json(data,status=200,headers={}){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...headers}})}
function authorized(request,env){if(!env.ADMIN_TOKEN)return false;return (request.headers.get('authorization')||'')===`Bearer ${env.ADMIN_TOKEN}`}
function requireAdmin(request,env){return authorized(request,env)?null:json({ok:false,error:'unauthorized'},401)}
async function body(request){try{return await request.json()}catch{return null}}
function now(){return new Date().toISOString()}
function safeId(v){return String(v||'').trim().replace(/[^A-Za-z0-9_-]/g,'').slice(0,40)}

async function tableNames(db){return ((await db.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY name").all()).results||[]).map(x=>x.name)}
async function columns(db,table){try{return ((await db.prepare(`PRAGMA table_info(${table})`).all()).results||[]).map(x=>x.name)}catch{return[]}}
async function schemaStatus(db){const tables=await tableNames(db);const ci=tables.includes('content_items')?await columns(db,'content_items'):[];return{tables,content_items_columns:ci,v2:tables.includes('content_items')&&tables.includes('content_versions'),v3:ci.includes('quote_type')&&tables.includes('media_inbox'),v4:tables.includes('languages')?!!(await db.prepare("SELECT 1 AS ok FROM languages WHERE code='fr'").first()):false,v5:ci.includes('source_verified_at')&&tables.includes('quote_source_evidence')}}
async function assetText(request,env,path){const r=await env.ASSETS.fetch(new Request(new URL(path,request.url)));if(!r.ok)throw new Error(`${path} unavailable (${r.status})`);return r.text()}
async function applyMigrations(request,env){const before=await schemaStatus(env.DB);const applied=[];
  if(!before.v2) return json({ok:false,error:'base schema v2 is missing; refusing destructive/implicit bootstrap',schema:before},409);
  let s=before;
  if(!s.v3){await env.DB.exec(await assetText(request,env,'/db/schema_v3.sql'));applied.push('v3');s=await schemaStatus(env.DB)}
  if(!s.v4){await env.DB.exec(await assetText(request,env,'/db/migration4_prepare_fr_and_cutover.sql'));applied.push('v4');s=await schemaStatus(env.DB)}
  if(!s.v5){await env.DB.exec(await assetText(request,env,'/db/migration5_content_origin.sql'));applied.push('v5');s=await schemaStatus(env.DB)}
  return json({ok:true,applied,schema:s});
}

async function nextContentId(env){const rows=(await env.DB.prepare("SELECT id FROM content_items WHERE id LIKE 'WQ%' ORDER BY CAST(SUBSTR(id,3) AS INTEGER) DESC LIMIT 1").all()).results||[];const n=rows.length?Number(String(rows[0].id).slice(2))+1:1;return `WQ${String(Number.isFinite(n)?n:1).padStart(3,'0')}`}
async function listContent(env){const r=await env.DB.prepare(`SELECT id,canonical_title,status,quote_type,author_name,original_language,created_at FROM content_items WHERE project_id=? ORDER BY COALESCE(sequence_no,0) DESC, created_at DESC LIMIT 100`).bind(PROJECT_ID).all();return r.results||[]}
async function createContent(request,env){const b=await body(request);if(!b?.quote)return json({ok:false,error:'quote required'},400);const type=b.quote_type==='verbatim'?'verbatim':'adapted';if(type==='verbatim'&&!String(b.author_name||'').trim())return json({ok:false,error:'author required for verbatim quote'},400);const id=safeId(b.content_id)||await nextContentId(env);const exists=await env.DB.prepare('SELECT 1 AS ok FROM content_items WHERE id=?').bind(id).first();if(exists)return json({ok:false,error:'content_id already exists',id},409);const seq=Number(String(id).replace(/^WQ/i,''))||null;await env.DB.prepare(`INSERT INTO content_items(id,project_id,content_type,sequence_no,category,canonical_title,source_text,source_name,source_url,status,facts_verified,uniqueness_verified,notes,created_at,updated_at,quote_type,original_quote,original_language,author_name,author_source,source_work,source_date,attribution_status,category_slug) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,PROJECT_ID,'quote',seq,b.category_slug||null,b.title||String(b.quote).slice(0,100),b.quote,b.author_name||null,b.source_url||b.author_source||null,'idea',type==='verbatim'?0:1,0,b.notes||null,now(),now(),type,b.quote,b.original_language||'en',b.author_name||null,b.author_source||b.source_url||null,b.source_work||null,b.source_date||null,type==='verbatim'?'unverified':'not_required',b.category_slug||null).run();return json({ok:true,id,item:await getContent(env,id)},201)}
async function getContent(env,id){const item=await env.DB.prepare(`SELECT * FROM content_items WHERE id=? AND project_id=?`).bind(id,PROJECT_ID).first();if(!item)return null;const versions=(await env.DB.prepare(`SELECT * FROM content_versions WHERE content_id=? ORDER BY language_code,version DESC`).bind(id).all()).results||[];let evidence=[];try{evidence=(await env.DB.prepare(`SELECT * FROM quote_source_evidence WHERE content_item_id=? ORDER BY id DESC`).bind(id).all()).results||[]}catch{}return{...item,versions,source_evidence:evidence}}
async function updateContent(request,env,id){const b=await body(request);const current=await env.DB.prepare(`SELECT * FROM content_items WHERE id=? AND project_id=?`).bind(id,PROJECT_ID).first();if(!current)return json({ok:false,error:'not found'},404);const type=b.quote_type??current.quote_type;const author=('author_name'in b)?b.author_name:current.author_name;if(type==='verbatim'&&!String(author||'').trim())return json({ok:false,error:'author required for verbatim quote'},400);await env.DB.prepare(`UPDATE content_items SET canonical_title=?,original_quote=?,source_text=?,original_language=?,author_name=?,author_source=?,source_work=?,source_date=?,source_url=?,quote_type=?,category=?,category_slug=?,attribution_status=?,notes=?,updated_at=? WHERE id=? AND project_id=?`).bind(b.title??current.canonical_title,b.quote??current.original_quote,b.quote??current.source_text,b.original_language??current.original_language,author||null,b.author_source??current.author_source,b.source_work??current.source_work,b.source_date??current.source_date,b.source_url??current.source_url,type,b.category_slug??current.category,b.category_slug??current.category_slug,b.attribution_status??current.attribution_status,b.notes??current.notes,now(),id,PROJECT_ID).run();return json({ok:true,item:await getContent(env,id)})}

async function mediaList(env){try{return (await env.DB.prepare(`SELECT * FROM media_inbox WHERE project_id=? ORDER BY created_at DESC LIMIT 100`).bind(PROJECT_ID).all()).results||[]}catch{return[]}}
async function mediaUpload(request,env){if(!env.MEDIA)return json({ok:false,error:'R2 binding MEDIA unavailable'},503);const form=await request.formData(),file=form.get('file');if(!file||typeof file==='string')return json({ok:false,error:'file required'},400);const lang=String(form.get('language_code')||'').toLowerCase();if(lang&&!LANGS.includes(lang))return json({ok:false,error:'unsupported language'},400);const mediaId=crypto.randomUUID();const filename=String(file.name||'upload.bin').replace(/[^a-zA-Z0-9._-]/g,'_');const key=`uploads/${new Date().toISOString().slice(0,10)}/${mediaId}-${filename}`;await env.MEDIA.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type||'application/octet-stream'}});try{await env.DB.prepare(`INSERT INTO media_inbox(id,project_id,content_item_id,content_version_id,r2_key,original_filename,asset_type,language_code,mime_type,size_bytes,status,uploaded_via,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(mediaId,PROJECT_ID,form.get('content_item_id')||null,null,key,file.name,form.get('asset_type')||'unknown',lang||null,file.type||null,file.size,'unassigned','admin',now(),now()).run()}catch(e){await env.MEDIA.delete(key);return json({ok:false,error:'db_insert_failed_r2_rolled_back',detail:String(e.message||e)},500)}return json({ok:true,id:mediaId,key,name:file.name},201)}

function adminHtml(){return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Wise Quotes Admin</title><style>body{font-family:system-ui;margin:0;background:#f5f5f3;color:#171717}.wrap{max-width:1000px;margin:auto;padding:20px}section{background:white;padding:18px;border-radius:16px;margin:14px 0}input,select,textarea,button{font:inherit;padding:10px;margin:5px 0;box-sizing:border-box}input,select,textarea{width:100%}button{cursor:pointer}.grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}pre{white-space:pre-wrap;overflow:auto}@media(max-width:650px){.grid{grid-template-columns:1fr}}</style></head><body><div class="wrap"><h1>Wise Quotes World — Admin</h1><section><label>Admin token</label><input id="token" type="password" autocomplete="current-password"><button onclick="connect()">Connect / status</button><button onclick="migrate()">Apply safe migrations</button><pre id="status"></pre></section><section><h2>New quote</h2><div class="grid"><div><label>Content ID (optional)</label><input id="cid" placeholder="auto: WQ006"></div><div><label>Type</label><select id="type"><option value="adapted">Adapted / Wise Quotes</option><option value="verbatim">Verbatim / attributed</option></select></div></div><label>Original quote</label><textarea id="quote" rows="4"></textarea><div class="grid"><div><label>Author</label><input id="author"></div><div><label>Original language</label><select id="olang">${LANGS.map(x=>`<option value="${x}">${x}</option>`).join('')}</select></div></div><label>Source URL / bibliographic source</label><input id="source"><label>Category</label><input id="category"><button onclick="createQuote()">Save to D1</button><pre id="createOut"></pre></section><section><h2>Media Inbox</h2><div class="grid"><input id="media" type="file"><select id="mlang">${LANGS.map(x=>`<option value="${x}">${x}</option>`).join('')}</select></div><select id="atype"><option value="video">Video</option><option value="pinterest">Pinterest 2:3</option><option value="image">Image</option></select><input id="mediaCid" placeholder="Content ID, e.g. WQ006"><button onclick="upload()">Upload to R2</button><pre id="uploadOut"></pre></section><section><h2>Content</h2><button onclick="loadAll()">Refresh</button><pre id="content"></pre></section></div><script>const H=(json=true)=>json?{'authorization':'Bearer '+token.value,'content-type':'application/json'}:{'authorization':'Bearer '+token.value};async function out(r,el){let x;try{x=await r.json()}catch{x={status:r.status}};el.textContent=JSON.stringify(x,null,2);return x}async function connect(){await out(await fetch('/api/admin/schema',{headers:H(false)}),status);await loadAll()}async function migrate(){await out(await fetch('/api/admin/migrate',{method:'POST',headers:H(false)}),status);await loadAll()}async function loadAll(){await out(await fetch('/api/admin/content',{headers:H(false)}),content)}async function createQuote(){const b={content_id:cid.value,quote_type:type.value,quote:quote.value,author_name:author.value||null,author_source:source.value||null,source_url:source.value||null,category_slug:category.value||null,original_language:olang.value};await out(await fetch('/api/admin/content',{method:'POST',headers:H(),body:JSON.stringify(b)}),createOut);await loadAll()}async function upload(){if(!media.files[0])return;const f=new FormData();f.append('file',media.files[0]);f.append('language_code',mlang.value);f.append('asset_type',atype.value);if(mediaCid.value)f.append('content_item_id',mediaCid.value);await out(await fetch('/api/admin/media',{method:'POST',headers:H(false),body:f}),uploadOut)}</script></body></html>`}

async function api(request,env,url){if(url.pathname==='/api/health')return json({ok:true,project:PROJECT_ID,languages:LANGS,db:!!env.DB,r2:!!env.MEDIA,admin_secret:!!env.ADMIN_TOKEN});if(!env.DB)return json({ok:false,error:'DB binding unavailable'},503);if(url.pathname.startsWith('/api/admin/')){const deny=requireAdmin(request,env);if(deny)return deny;if(url.pathname==='/api/admin/schema'&&request.method==='GET')return json({ok:true,schema:await schemaStatus(env.DB)});if(url.pathname==='/api/admin/migrate'&&request.method==='POST')return applyMigrations(request,env);if(url.pathname==='/api/admin/content'&&request.method==='GET')return json({ok:true,items:await listContent(env)});if(url.pathname==='/api/admin/content'&&request.method==='POST')return createContent(request,env);const m=url.pathname.match(/^\/api\/admin\/content\/([A-Za-z0-9_-]+)$/);if(m&&request.method==='GET'){const x=await getContent(env,m[1]);return x?json({ok:true,item:x}):json({ok:false,error:'not found'},404)}if(m&&request.method==='PATCH')return updateContent(request,env,m[1]);if(url.pathname==='/api/admin/media'&&request.method==='GET')return json({ok:true,items:await mediaList(env)});if(url.pathname==='/api/admin/media'&&request.method==='POST')return mediaUpload(request,env)}return json({ok:false,error:'not found'},404)}

export default{async fetch(request,env){const url=new URL(request.url);try{if(url.pathname==='/admin'||url.pathname==='/admin/')return new Response(adminHtml(),{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-robots-tag':'noindex,nofollow'}});if(url.pathname.startsWith('/api/'))return api(request,env,url);if(url.pathname==='/'||url.pathname==='/index.html')return Response.redirect(`${url.origin}/${preferred(request)}/`,302);return env.ASSETS.fetch(request)}catch(e){return json({ok:false,error:String(e?.message||e)},500)}}};
